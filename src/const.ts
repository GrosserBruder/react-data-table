export enum SORT_VALUES {
  NOT_SELECTED = 'not_selected',
  ASC = 'asc',
  DESC = 'desc'
}

export const enum FILTER_TYPES {
  DATE = "date",
  NUMBER = "number",
  BOOLEAN = "boolean",
  STRING = "string",
}