export enum SELECT_STATUSES {
  NOT_SELECTED,
  INDETERMINATE,
  SELECTED
}

export enum SORT_STRATEGY {
  ASC = "asc",
  DESC = "desc",
}

export enum SORTING_MODE {
  OFF = "off",
  SINGLE = "single",
  MULTIPLE = "multiple"
}