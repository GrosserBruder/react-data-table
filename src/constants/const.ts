export enum SELECT_STATUSES {
  NOT_SELECTED,
  INDETERMINATE,
  SELECTED
}

export enum SORTING_ORDER {
  ASC = "asc",
  DESC = "desc",
}

export enum SORTING_MODE {
  SERVER = "server",
  CLIENT = "client",
}

export enum FILTERING_MODE {
  SERVER = "server",
  CLIENT = "client",
}